apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9090
    isDefault: true
    editable: false
    uid: DS_PROMETHEUS

  - name: Loki
    type: loki
    access: proxy
    url: http://localhost:3100
    editable: false
    uid: DS_LOKI
